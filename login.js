document.getElementById("btnEntrar").addEventListener("click", (event) => {
    event.preventDefault(); // impede o form de recarregar a página
    window.location.href = "index.html"; // redireciona normalmente
});
